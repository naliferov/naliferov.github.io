//chain of triggers on hard drive, no process programming, you don't need repub resub, but nobody knows what the structure of the program
const f = {};

//functions start
//functions stop

export const X = async (x) => {
  if (x.callFuncName) {
    //net get and call code
    //return await [x.func](x);
  }
  if (x.func && x.funcName) {
    //add to functions start
  }
};

const { promises } = await import('node:fs');
const fs = promises;

console.dir({ x: 2, y: () => { } })